import styled from "styled-components";

const EndComponentWrapper = styled.div`

  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  color: #999; /* 根据您的图片，文本颜色看起来是浅灰色 */
  font-size: 14px; /* 调整为您希望的字体大小 */
  margin: 20px 0; /* 上下间距，根据需要调整 */
.hint{
    height: 100px;
    width: 300px;
}
`;
export default EndComponentWrapper